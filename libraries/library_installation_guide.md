Use venv then pip install then run file with python/python3

*more information on venv and pip and python3 in their respective folders*

Or be smart and use uv  
uv init or uv sync  
uv add dependency  
uv run file.py

*more information on uv in the uv folder*
